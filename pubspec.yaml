name: delivery
description: "Delivery app with Supabase backend"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # SUPABASE
  supabase_flutter: ^2.5.6

  # STATE MANAGEMENT
  provider: ^6.1.5

  # UI & UTILS
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.7
  oktoast: ^3.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
